let x = parseInt(prompt("Entrez un nombre :"));
document.write("Les diviseurs de X = " + x + " :<br>");
if (x % i === 0);
for (let i = 1; i <= x; i++) {
  if (x % i === 0) {
    document.write(i + " ");
  }
}

